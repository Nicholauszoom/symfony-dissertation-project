<form method="post" action="{{ path('app_index_detail_delete', {'id': index_detail.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ index_detail.id) }}">
    <button class="btn">Delete</button>
</form>
