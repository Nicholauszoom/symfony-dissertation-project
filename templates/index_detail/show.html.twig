{% extends 'base.html.twig' %}

{% block title %}IndexDetail{% endblock %}

{% block body %}
    <h1>IndexDetail</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ index_detail.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ index_detail.title }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ index_detail.description }}</td>
            </tr>
            <tr>
                <th>ImagePath</th>
                <td>{{ index_detail.imagePath }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_index_detail_index') }}">back to list</a>

    <a href="{{ path('app_index_detail_edit', {'id': index_detail.id}) }}">edit</a>

    {{ include('index_detail/_delete_form.html.twig') }}
{% endblock %}
