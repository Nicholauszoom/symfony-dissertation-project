<form method="post" action="{{ path('app_infrastructure_delete', {'id': infrastructure.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ infrastructure.id) }}">
    {# <button class="btn">Delete</button> #}
   <a class="bg-red-500 font-bold py-2 px-4 rounded transition-all hover:bg-red-300">Delete</a>
</form>
