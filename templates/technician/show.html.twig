{% extends 'base2.html.twig' %}

{% block title %}Technician{% endblock %}

{% block body %}
    <h1>Technician</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ technician.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ technician.name }}</td>
            </tr>
            <tr>
                <th>Phone</th>
                <td>{{ technician.phone }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ technician.email }}</td>
            </tr>
            <tr>
                <th>Profession</th>
                <td>{{ technician.profession }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_technician_index') }}">back to list</a>

    <a href="{{ path('app_technician_edit', {'id': technician.id}) }}">edit</a>

    {{ include('technician/_delete_form.html.twig') }}
{% endblock %}
